import {CardProps} from "./characters.types";
import {LocationProps} from "./location.types";
import {EpisodeTypes} from "./episode.types";
import {ParamsProps,LocationParamsProps} from "./ParamsProps";

export type {
    CardProps,
    LocationProps,
    EpisodeTypes,
    ParamsProps,
    LocationParamsProps
}

export {}